# Week-1

| № | Задача                               | Формулировка запроса                                                                                                | Результат                                                                                         |
|---|--------------------------------------|---------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|
| 1 | Понимание принципов Windows-скриптов | Попросил объяснить назначение и структуру `.bat`-файлов, а также их роль в автоматизации задач в Windows            | Получил системное объяснение пакетных файлов и примеры их практического применения                |
| 2 | Автоматизация настройки окружения    | Запросил помощь в разработке `setup_env.bat` для автоматического создания Conda-окружения и установки зависимостей  | Сформирован рабочий шаблон скрипта с проверкой окружения, установкой зависимостей и smoke-test    |
| 3 | Детальный разбор логики скрипта      | Попросил пошагово объяснить структуру и команды внутри созданного `.bat`-файла                                      | Получено подробное описание логики выполнения, обработки ошибок и принципа идемпотентности        |
| 4 | Диагностика Conda в Windows          | Уточнил причину ошибки `Conda not found in PATH` и возможные способы её устранения                                  | Разобрана проблема PATH, предложены решения через `conda init` и альтернативный поиск `conda.bat` |
| 5 | Повышение устойчивости проекта       | Попросил улучшить скрипт так, чтобы он корректно работал как на локальной машине, так и при проверке преподавателем | Реализован более надёжный механизм поиска Conda и повышена переносимость проекта                  |
| 6 | Финальная верификация решения        | Запросил оценку корректности итогового `setup_env.bat`                                                              | Подтверждена корректность логики и соответствие требованиям воспроизводимости                     |

# Week-2

| №  | Задача                                 | Формулировка запроса                                                                                                                                                                         | Результат                                                                                                                                                            |
|----|----------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 7  | Проектирование Extract                 | Сформулировал задачу реализации скрипта `extract.py`, который должен читать параметры из YAML-конфига, выполнять HTTP-запрос к API World Bank и сохранять необработанные данные в `data/raw` | Спроектирована архитектура Extract-скрипта: конфигурационный подход, использование `requests`, обработка сетевых ошибок и сохранение raw-данных с временными метками |
| 8  | Работа с конфигурационными файлами     | Запросил реализацию чтения настроек из `variant_09.yml`, описывающего источник данных, API-метод, параметры запроса и схему нормализации                                                     | Реализован механизм загрузки YAML-конфигурации через `PyYAML`, обеспечивающий гибкость и масштабируемость ETL-пайплайна                                              |
| 9  | Интеграция с World Bank API            | Поставил задачу выполнить HTTP-запрос к публичному API Всемирного банка для получения данных по индикатору ВВП России                                                                        | Реализован универсальный HTTP-запрос с поддержкой параметров API, таймаутов и обработки исключений (`Timeout`, `ConnectionError`, `HTTPError`)                       |
| 10 | Организация хранения raw-данных        | Уточнил необходимость сохранения исходных данных с указанием даты и времени загрузки                                                                                                         | Реализована структура хранения `data/raw/variant_XX/` и автоматическая генерация файлов `raw_YYYYMMDD_HHMMSS.json`                                                   |
| 11 | Диагностика ошибки YAML-парсинга       | Провёл анализ ошибки `yaml.parser.ParserError`, возникающей при запуске скрипта через `conda run`                                                                                            | Выявлено, что YAML-парсер получает путь к `extract.py` вместо конфигурационного файла, что вызывает попытку интерпретации Python-кода как YAML                       |
| 12 | Отладка передачи аргументов CLI        | Проверил корректность передачи аргументов командной строки и сообщил результаты диагностики                                                                                                  | Определено место потенциальной подмены пути к конфигу и предложены методы отладки (`print(sys.argv)`, проверка `load_config`, контроль Run-конфигурации PyCharm)     |
| 13 | Повышение устойчивости Extract-скрипта | Инициировал обсуждение улучшений обработки конфигурации и путей файлов                                                                                                                       | Предложены улучшения: использование `Path.resolve()`, проверка расширения `.yml`, и защита от некорректных путей к конфигу                                           |

# Week-3

| №  | Задача                                     | Формулировка запроса                                                                                                      | Результат                                                                                                                                                       |
|----|--------------------------------------------|---------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 14 | Анализ структуры JSON                      | Запросил помощь в анализе структуры raw JSON, полученного из API World Bank, для определения подходящей структуры таблицы | Определено, что ответ API содержит два элемента: метаданные и список наблюдений; принято решение использовать второй элемент массива как источник строк таблицы |
| 15 | Проектирование нормализации данных         | Сформулировал задачу преобразования raw JSON в табличную структуру для последующего анализа                               | Предложена стратегия нормализации: использование `pandas.json_normalize` для разворачивания вложенных объектов `country` и `indicator`                          |
| 16 | Реализация преобразования JSON → DataFrame | Запросил пример кода для преобразования структуры JSON в `pandas DataFrame`                                               | Реализован код загрузки JSON, извлечения массива наблюдений и построения DataFrame                                                                              |
| 17 | Очистка и подготовка данных                | Поставил задачу выполнения базовой очистки данных и приведения типов столбцов                                             | Реализованы шаги очистки: приведение `date` к типу `datetime`, преобразование `value` в `float`, удаление строк с `NULL` значениями GDP и устранение дубликатов |
| 18 | Проектирование normalized-слоя             | Запросил помощь в описании схемы нормализованных данных и определении зерна таблицы                                       | Определена структура normalized-таблицы и правило: одна строка соответствует значению индикатора GDP для конкретной страны и года                               |
| 19 | Документирование схемы данных              | Поставил задачу дополнить `Data_Contract.md` схемой normalized-слоя                                                       | Сформирована таблица схемы данных с указанием типов, nullable-полей, источников полей в raw JSON и описания                                                     |
| 20 | Обновление документации проекта            | Запросил помощь в обновлении README и Implementation Plan для отражения нового этапа ETL                                  | README и Implementation Plan дополнены описанием этапа Transform, структуры normalized-данных и pipeline проекта                                                |

# Week-4

| №  | Задача                            | Формулировка запроса                                                                        | Результат                                                                                                                           |
|----|-----------------------------------|---------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------|
| 21 | Понимание задачи витрины (mart)   | Запросил разъяснение требований к построению витрины данных на основе normalized-слоя       | Определено, что mart-слой представляет агрегированную таблицу с KPI, временной динамикой и обогащением справочными данными          |
| 22 | Проектирование структуры mart     | Сформулировал задачу определения структуры витрины и набора метрик                          | Выбрана структура витрины с агрегированными KPI: последнее значение, изменение за 10 лет, среднее за 10 лет, максимум и минимум     |
| 23 | Подготовка временной оси          | Поставил задачу корректной подготовки временного поля для расчётов                          | Выполнена сортировка по `year`, определена годовая гранулярность данных, обеспечена корректная последовательность временного ряда   |
| 24 | Расчёт KPI                        | Запросил реализацию вычисления метрик из конфигурации (latest, delta_10y, avg_10y, max/min) | Реализован расчёт KPI с учётом обработки пропусков, проверки наличия данных за 10 лет и корректной агрегации по временной оси       |
| 24 | Обработка предупреждений pandas   | Провёл анализ предупреждения `FutureWarning` при использовании `pct_change()`               | Выявлена причина изменения поведения pandas, предложено использование `fill_method=None` для корректной и явной обработки пропусков |
| 25 | Формирование итоговой витрины     | Сформулировал задачу создания итоговой таблицы mart                                         | Реализована витрина в виде одной строки с агрегированными KPI, соответствующая требованиям задания                                  |
| 27 | Обновление Implementation Plan    | Запросил помощь в дополнении документации этапом Mart                                       | Implementation Plan дополнен описанием mart-слоя, расчёта KPI и обоснованием разделения слоёв                                       |
| 28 | Обновление Data Contract и README | Поставил задачу актуализации документации проекта под новую архитектуру                     | Обновлены Data Contract и README: добавлено описание normalized-схемы, mart-слоя и полного pipeline (Extract → Transform → Mart)    |

# Week-5

| №  | Задача                         | Формулировка запроса                                                                  | Результат                                                                                                                                         |
|----|--------------------------------|---------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------|
| 29 | Диагностика пустой таблицы     | Запросил объяснение причин пустой таблицы в SQLite/Postgres (commit, не та база)      | Понял: без `commit()` данные откатываются при `close()`, `os.path.abspath()` показывает реальный путь БД; изучил `engine.begin()` для авто-commit |
| 30 | Подъем Postgres в Docker       | Запросил пошаговую инструкцию запуска PostgreSQL через Docker для Windows             | Реализован запуск через docker-compose up -d                                                                                                      |
| 31 | Решение проблем Docker Desktop | Столкнулся с ошибками "500 Internal Server Error" и "Virtualization disabled"         | Решены: перезапуск Docker Engine, включение VT-x в BIOS, предложены альтернативы (PostgreSQL installer)                                           |
| 32 | Адаптация load_mart.py под CSV | Запросил обновление скрипта загрузки под конкретные пути `data/mart/variant_09/*.csv` | Создан универсальный `load_mart.py`: чтение CSV, автоопределение схемы, `DROP+to_sql`, проверки shape/columns                                     |
| 33 | Фикс SQLAlchemy типов данных   | Ошибка `syntax error at "int64/str"` при ручном CREATE TABLE                          | Переход на `df.to_sql()` с автоопределением типов PostgreSQL (`INTEGER`, `TEXT`, `DOUBLE PRECISION`)                                              |
| 34 | Создание sql_checkt.py         | Запросил отдельный скрипт с 5+ SQL-проверками по заданию                              | Создан `sql_check.py`: count, диапазон, NULL, дубли, KPI (AVG(gdp_growth_pct)), уникальные страны                                                 |
| 35 | Фикс PostgreSQL ROUND()        | Ошибка `function round(double precision, integer) does not exist`                     | Исправлено: `ROUND(AVG()::numeric, 2)` вместо `ROUND(AVG(double), 2)`                                                                             |
| 36 | Параллельная загрузка 2 таблиц | Запросил поддержку `mart_yearly_*.csv` + `mart_stats_*.csv`                           | Обновлен `load_mart.py`: цикл по файлам, маппинг `TABLE_NAMES`, загрузка в `russia_gdp` + `russia_gdp_stats`                                      |
