# Week-1

| № | Задача                               | Формулировка запроса                                                                                                | Результат                                                                                         |
|---|--------------------------------------|---------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|
| 1 | Понимание принципов Windows-скриптов | Попросил объяснить назначение и структуру `.bat`-файлов, а также их роль в автоматизации задач в Windows            | Получил системное объяснение пакетных файлов и примеры их практического применения                |
| 2 | Автоматизация настройки окружения    | Запросил помощь в разработке `setup_env.bat` для автоматического создания Conda-окружения и установки зависимостей  | Сформирован рабочий шаблон скрипта с проверкой окружения, установкой зависимостей и smoke-test    |
| 3 | Детальный разбор логики скрипта      | Попросил пошагово объяснить структуру и команды внутри созданного `.bat`-файла                                      | Получено подробное описание логики выполнения, обработки ошибок и принципа идемпотентности        |
| 4 | Диагностика Conda в Windows          | Уточнил причину ошибки `Conda not found in PATH` и возможные способы её устранения                                  | Разобрана проблема PATH, предложены решения через `conda init` и альтернативный поиск `conda.bat` |
| 5 | Повышение устойчивости проекта       | Попросил улучшить скрипт так, чтобы он корректно работал как на локальной машине, так и при проверке преподавателем | Реализован более надёжный механизм поиска Conda и повышена переносимость проекта                  |
| 6 | Финальная верификация решения        | Запросил оценку корректности итогового `setup_env.bat`                                                              | Подтверждена корректность логики и соответствие требованиям воспроизводимости                     |

# Week-2

| №  | Задача                                 | Формулировка запроса                                                                                                                                                                         | Результат                                                                                                                                                            |
|----|----------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 7  | Проектирование Extract                 | Сформулировал задачу реализации скрипта `extract.py`, который должен читать параметры из YAML-конфига, выполнять HTTP-запрос к API World Bank и сохранять необработанные данные в `data/raw` | Спроектирована архитектура Extract-скрипта: конфигурационный подход, использование `requests`, обработка сетевых ошибок и сохранение raw-данных с временными метками |
| 8  | Работа с конфигурационными файлами     | Запросил реализацию чтения настроек из `variant_09.yml`, описывающего источник данных, API-метод, параметры запроса и схему нормализации                                                     | Реализован механизм загрузки YAML-конфигурации через `PyYAML`, обеспечивающий гибкость и масштабируемость ETL-пайплайна                                              |
| 9  | Интеграция с World Bank API            | Поставил задачу выполнить HTTP-запрос к публичному API Всемирного банка для получения данных по индикатору ВВП России                                                                        | Реализован универсальный HTTP-запрос с поддержкой параметров API, таймаутов и обработки исключений (`Timeout`, `ConnectionError`, `HTTPError`)                       |
| 10 | Организация хранения raw-данных        | Уточнил необходимость сохранения исходных данных с указанием даты и времени загрузки                                                                                                         | Реализована структура хранения `data/raw/variant_XX/` и автоматическая генерация файлов `raw_YYYYMMDD_HHMMSS.json`                                                   |
| 11 | Диагностика ошибки YAML-парсинга       | Провёл анализ ошибки `yaml.parser.ParserError`, возникающей при запуске скрипта через `conda run`                                                                                            | Выявлено, что YAML-парсер получает путь к `extract.py` вместо конфигурационного файла, что вызывает попытку интерпретации Python-кода как YAML                       |
| 12 | Отладка передачи аргументов CLI        | Проверил корректность передачи аргументов командной строки и сообщил результаты диагностики                                                                                                  | Определено место потенциальной подмены пути к конфигу и предложены методы отладки (`print(sys.argv)`, проверка `load_config`, контроль Run-конфигурации PyCharm)     |
| 13 | Повышение устойчивости Extract-скрипта | Инициировал обсуждение улучшений обработки конфигурации и путей файлов                                                                                                                       | Предложены улучшения: использование `Path.resolve()`, проверка расширения `.yml`, и защита от некорректных путей к конфигу                                           |

# Week-3

| №  | Задача                                     | Формулировка запроса                                                                                                      | Результат                                                                                                                                                       |
|----|--------------------------------------------|---------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 14 | Анализ структуры JSON                      | Запросил помощь в анализе структуры raw JSON, полученного из API World Bank, для определения подходящей структуры таблицы | Определено, что ответ API содержит два элемента: метаданные и список наблюдений; принято решение использовать второй элемент массива как источник строк таблицы |
| 15 | Проектирование нормализации данных         | Сформулировал задачу преобразования raw JSON в табличную структуру для последующего анализа                               | Предложена стратегия нормализации: использование `pandas.json_normalize` для разворачивания вложенных объектов `country` и `indicator`                          |
| 16 | Реализация преобразования JSON → DataFrame | Запросил пример кода для преобразования структуры JSON в `pandas DataFrame`                                               | Реализован код загрузки JSON, извлечения массива наблюдений и построения DataFrame                                                                              |
| 17 | Очистка и подготовка данных                | Поставил задачу выполнения базовой очистки данных и приведения типов столбцов                                             | Реализованы шаги очистки: приведение `date` к типу `datetime`, преобразование `value` в `float`, удаление строк с `NULL` значениями GDP и устранение дубликатов |
| 18 | Проектирование normalized-слоя             | Запросил помощь в описании схемы нормализованных данных и определении зерна таблицы                                       | Определена структура normalized-таблицы и правило: одна строка соответствует значению индикатора GDP для конкретной страны и года                               |
| 19 | Документирование схемы данных              | Поставил задачу дополнить `Data_Contract.md` схемой normalized-слоя                                                       | Сформирована таблица схемы данных с указанием типов, nullable-полей, источников полей в raw JSON и описания                                                     |
| 20 | Обновление документации проекта            | Запросил помощь в обновлении README и Implementation Plan для отражения нового этапа ETL                                  | README и Implementation Plan дополнены описанием этапа Transform, структуры normalized-данных и pipeline проекта                                                |

# Week-4

| №  | Задача                            | Формулировка запроса                                                                        | Результат                                                                                                                           |
|----|-----------------------------------|---------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------|
| 21 | Понимание задачи витрины (mart)   | Запросил разъяснение требований к построению витрины данных на основе normalized-слоя       | Определено, что mart-слой представляет агрегированную таблицу с KPI, временной динамикой и обогащением справочными данными          |
| 22 | Проектирование структуры mart     | Сформулировал задачу определения структуры витрины и набора метрик                          | Выбрана структура витрины с агрегированными KPI: последнее значение, изменение за 10 лет, среднее за 10 лет, максимум и минимум     |
| 23 | Подготовка временной оси          | Поставил задачу корректной подготовки временного поля для расчётов                          | Выполнена сортировка по `year`, определена годовая гранулярность данных, обеспечена корректная последовательность временного ряда   |
| 24 | Расчёт KPI                        | Запросил реализацию вычисления метрик из конфигурации (latest, delta_10y, avg_10y, max/min) | Реализован расчёт KPI с учётом обработки пропусков, проверки наличия данных за 10 лет и корректной агрегации по временной оси       |
| 24 | Обработка предупреждений pandas   | Провёл анализ предупреждения `FutureWarning` при использовании `pct_change()`               | Выявлена причина изменения поведения pandas, предложено использование `fill_method=None` для корректной и явной обработки пропусков |
| 25 | Формирование итоговой витрины     | Сформулировал задачу создания итоговой таблицы mart                                         | Реализована витрина в виде одной строки с агрегированными KPI, соответствующая требованиям задания                                  |
| 27 | Обновление Implementation Plan    | Запросил помощь в дополнении документации этапом Mart                                       | Implementation Plan дополнен описанием mart-слоя, расчёта KPI и обоснованием разделения слоёв                                       |
| 28 | Обновление Data Contract и README | Поставил задачу актуализации документации проекта под новую архитектуру                     | Обновлены Data Contract и README: добавлено описание normalized-схемы, mart-слоя и полного pipeline (Extract → Transform → Mart)    |

# Week-5

| №  | Задача                         | Формулировка запроса                                                                  | Результат                                                                                                                                         |
|----|--------------------------------|---------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------|
| 29 | Диагностика пустой таблицы     | Запросил объяснение причин пустой таблицы в SQLite/Postgres (commit, не та база)      | Понял: без `commit()` данные откатываются при `close()`, `os.path.abspath()` показывает реальный путь БД; изучил `engine.begin()` для авто-commit |
| 30 | Подъем Postgres в Docker       | Запросил пошаговую инструкцию запуска PostgreSQL через Docker для Windows             | Реализован запуск через docker-compose up -d                                                                                                      |
| 31 | Решение проблем Docker Desktop | Столкнулся с ошибками "500 Internal Server Error" и "Virtualization disabled"         | Решены: перезапуск Docker Engine, включение VT-x в BIOS, предложены альтернативы (PostgreSQL installer)                                           |
| 32 | Адаптация load_mart.py под CSV | Запросил обновление скрипта загрузки под конкретные пути `data/mart/variant_09/*.csv` | Создан универсальный `load_mart.py`: чтение CSV, автоопределение схемы, `DROP+to_sql`, проверки shape/columns                                     |
| 33 | Фикс SQLAlchemy типов данных   | Ошибка `syntax error at "int64/str"` при ручном CREATE TABLE                          | Переход на `df.to_sql()` с автоопределением типов PostgreSQL (`INTEGER`, `TEXT`, `DOUBLE PRECISION`)                                              |
| 34 | Создание sql_checkt.py         | Запросил отдельный скрипт с 5+ SQL-проверками по заданию                              | Создан `sql_check.py`: count, диапазон, NULL, дубли, KPI (AVG(gdp_growth_pct)), уникальные страны                                                 |
| 35 | Фикс PostgreSQL ROUND()        | Ошибка `function round(double precision, integer) does not exist`                     | Исправлено: `ROUND(AVG()::numeric, 2)` вместо `ROUND(AVG(double), 2)`                                                                             |
| 36 | Параллельная загрузка 2 таблиц | Запросил поддержку `mart_yearly_*.csv` + `mart_stats_*.csv`                           | Обновлен `load_mart.py`: цикл по файлам, маппинг `TABLE_NAMES`, загрузка в `russia_gdp` + `russia_gdp_stats`                                      |

# Week-6

| №  | Задача                                     | Формулировка запроса                                                                                                   | Результат                                                                                                                                                                              |
|----|--------------------------------------------|------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 37 | Ввод пайплайна extract → transform → mart  | Запросил организацию полного ETL‑пайплайна с единым CLI‑модулем pipeline для full / incremental                        | Реализована структура src/extract.py, transform.py, mart.py, load.py и pipeline.py с вызовом python -m src.pipeline.pipeline --mode full/incremental                                   |
| 38 | Реализация full / incremental              | Запросил чёткое отличие режимов full и incremental, и пример логики обработки                                          | В full — полная перезагрузка (все данные, пересоздание normalized/mart); в incremental — обработка только новых данных по watermark из state.json                                      |
| 39 | Создание и использование state.json        | Запросил, как и где создавать data/state.json и как из него брать timestamp для transform / mart                       | state.json создаётся и обновляется в extract: туда пишется variant, timestamp, last_date, last_successful_run; transform и mart читают TIMESTAMP и variant из state                    |
| 40 | Перепись extract.py под pipeline           | Запросил переписать extract.py под вызов extract.extract_data(mode=args.mode) с генерацией файла в data/raw            | Написана функция extract_data(mode=..., config_path=...), которая запрашивает API, сохраняет data/raw/variant_XX/eaw_*.json и обновляет state.json с TIMESTAMP                         |
| 41 | Перепись transform.py                      | Запросил переписать transform.py так, чтобы он брал TIMESTAMP и variant из state.json вместо хардкода                  | transform_data() читает state.json, открывает data/raw/variant_XX/eaw_*.json, нормализует и сохраняет data/normalized/variant_XX/normalized_*.csv                                      |
| 42 | Перепись mart.py                           | Запросил переписать mart.py под чтение TIMESTAMP и variant из state.json                                               | mart_data() строит mart_yearly и mart_stats по data/normalized/variant_XX/normalized_*.csv и сохраняет в data/mart/variant_XX/                                                         |
| 43 | Перепись load.py под state и mode          | Запросил переписать load.py под чтение TIMESTAMP и variant из state и обработку full/incremental                       | load_data(mode=...) читает state.json, формирует пути к mart_yearly_*.csv / mart_stats_*.csv и загружает их в PostgreSQL, в full делает DROP TABLE, в incremental — if_exists='append' |
| 44 | Запуск команды с -m и mypackage            | Запросил реализацию python -m src.mypackage.pipeline --config ... и исправление ошибки No module named 'src.mypackage' | Добавлен pyproject.toml и pip install -e . в conda‑окружении, чтобы src.mypackage стал видимым пакетом для python -m                                                                   |
| 45 | Обработка таймаутов и 502 у World Bank API | Запросил исправление ReadTimeout и 502 Bad Gateway при обращении к api.worldbank.org                                   | Увеличен DEFAULT_TIMEOUT, добавлен retry в make_request; адаптирована логика extract_data под формат ответа API World Bank (list из двух элементов)                                    |
| 46 | Фикс AttributeError .get("items")          | Запросил переписать records = data.get("items", data) под случай, когда data — list                                    | Заменена строка на логику if isinstance(data, list) and len(data)==2: meta, records = data, чтобы не ломался .get() на списке                                                          |
| 47 | Тестирование полного pipeline              | Запросил, как запустить --mode full и убедиться, что state.json корректно обновляется                                  | Реализован полный цикл запуска extract → transform → mart → load через pipeline.py; после успешного full проверяется структура data/ и содержимое state.json                           |

***

# Week‑7

| №  | Задача                                   | Формулировка запроса                                                                                      | Результат                                                                                                                                                                                    |
|----|------------------------------------------|-----------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 48 | Структура `week7_viz.ipynb`              | Запросил шаблон структуры ноутбука с подготовкой данных, тремя вопросами и тремя графиками                | Получил организованный план: `1. Подготовка данных → 2. Три вопроса → 3. Временной ряд → 4. Распределение → 5. Ранжирование → 6. Выводы`.                                                    |
| 49 | Загрузка `mart_yearly` и проверка        | Запросил код для загрузки `mart_yearly_*.csv` и вывода `shape`, типов, `NaN` и `year`                     | Написан код с `pd.read_csv("data/mart/variant_09/...")`, проверкой `df.head()`, `df.isna().sum()` и `pd.to_datetime(df["year"], format="%Y")`.                                               |
| 50 | Временной ряд GDP по годам               | Запросил реализацию линейного графика `gdp` по `year` с сохранением в `docs/figures/week7_timeseries.png` | Построен `sns`/`matplotlib`-график `plt.plot(...)` с `plt.savefig("docs/figures/week7_timeseries.png")` и читаемыми подписями и сеткой.                                                      |
| 51 | Распределение GDP (density)              | Запросил альтернативу `histogram + boxplot` и объяснение, что означает “плотность”                        | Реализован `sns.kdeplot(...)` как `density plot`, добавлены `FuncFormatter`/`FormatStrFormatter` для улучшения оси `y` и комментарий, что плотность = нормализованная относительная частота. |
| 52 | Ранжирование топ‑5 / худшие 5 лет по GDP | Запросил `barplot` с топ‑5 и худшими 5 годами, разделёнными по `"type"`                                   | Реализован `sns.barplot(data=combined, y="year", x="gdp", hue="type")` с `pd.concat([...])` и `nlargest(5, "gdp")`/`nsmallest(5, "gdp")`.                                                    |
| 53 | Оформление выводов по графикам           | Запросил примеры трёх текстовых выводов, привязанных к каждому из трёх графиков                           | Сформулированы выводы: по временному ряду, распределению и ранжированию, с указанием трендов, асимметрии и конкретных лидирующих/отстающих лет.                                              |
| 54 | Сохранение и воспроизводимость           | Запросил, как сделать ноутбук полностью воспроизводимым и корректно сохранять графики в `docs/figures/`   | Введён `Path("../data/...")` и `mkdir(parents=True, exist_ok=True)` для `docs/figures`; подтверждено, что `Run All` в Jupyter даёт те же картинки без ручных правок.                         |

***

# Week‑8

| №  | Задача                                           | Формулировка запроса                                                                                | Результат                                                                                                                                                                                                                             |
|----|--------------------------------------------------|-----------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 55 | Структура src/ и tests/ для dq                   | Запросил рекомендованную структуру проекта: src/ с модулем dq и папка tests/ с test_dq.py           | Получил план: src/dq/__init__.py и src/dq/dq.py + tests/test_dq.py с импортом sys.path.insert для корректного from src.dq.dq import ....                                                                                              |
| 56 | Позитивный, негативный и граничный тесты dq      | Запросил три типа юнит‑тестов (позитив/негатив/граничный) для check_null_ratio и других dq‑проверок | Получил завершённый tests/test_dq.py с 6 тестами: test_dq_positively_clean_data, test_dq_with_null_gdp, test_dq_with_duplicated_key, test_dq_with_year_1940, test_dq_ten_percent_null_gdp_diff, test_dq_eleven_percent_null_gdp_diff. |
| 57 | Исправление ModuleNotFoundError: No module 'src' | Запросил, как поправить from src.dq.dq import ... при запуске pytest из корня проекта               | Получил решение: добавить sys.path.insert(0, ...) в tests/test_dq.py для корректного импорта src.                                                                                                                                     |
| 58 | Исправление check_null_ratio в dq.py             | Запросил, почему ratio = 0.0 и status = PASS при 10/11% NULL в gdp_diff                             | Получил уточнённую реализацию check_null_ratio и поправленные фейк‑датафреймы df_with_10_percent_null_gdp_diff и df_with_11_percent_null_gdp_diff с явными None.                                                                      |
| 59 | Запуск pytest через conda run                    | Запросил правильную команду для запуска tests/test_dq.py в окружении data_env                       | Получил команду conda run -n data_env pytest tests/test_dq.py -v и объяснение, что pytest корректно собирает и запускает все 6 тестов без ошибок.                                                                                     |
| 60 | Итоговая проверка корректности тестов dq         | Прислал вывод pytest с 6 passed in 0.46s и спросил, “пойдёт ли такой ответ”                         | Подтверждено, что вывод является корректным и полным: все тесты проходят, структура src/ + tests/ и dq.py полностью соответствуют требованиям LR‑2.                                                                                   |

***

# Week‑9

| №  | Задача                                            | Формулировка запроса                                                                                                                                            | Результат                                                                                                                                                                           |
|----|---------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 61 | Обновление Data_Contract.md под новую часть курса | Запросил, как привести старый Data_Contract.md в соответствие с новыми требованиями: указать гранулярность, время/TZ, единицы, changelog и Naming & Units rules | Получил обновлённый вариант Data_Contract.md с блоками normalized и mart, явными unit и dtype, временной зоной UTC, версией 0.1 и Changelog, а также разделом Naming & Units rules. |
| 62 | Создание docs/Data_dictionary.md по mart_yearly   | Прислал CSV mart_yearly и попросил оформить человеко‑ориентированный словарь данных по её колонкам year, country_name, gdp, gdp_diff, gdp_growth_pct            | Получил готовый data_dictionary.md с бизнес‑смыслом, единицами и примерами для каждой колонки, что делает mart понятным для непрограммистов и аналитиков.                           |

***

# Week‑10

| №  | Задача                                                  | Формулировка запроса                                                                                                                  | Результат                                                                                                                                                                                                                          |
|----|---------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 63 | Подготовка docker-compose.yml для Postgres + Metabase   | Запросил, как дописать к уже существующему postgres‑сервису metabase, оставив свои usrnme/123 и lab                                   | Получил обновлённый docker-compose.yml с postgres:16, lab и usrnme/123, а также volumes pgdata и metabase_data, с объяснением, как правильно подключаться к postgres и metabase изнутри Docker‑сети.                               |
| 64 | Подключение load.py к Postgres через docker-compose     | Спросил, как настроить src/load.py под подключение к lab с переменными DB_USER/DB_PASSWORD/DB_HOST и т.д.                             | Получил доработанную версию load.py с явной валидацией переменных окружения и сохранённой логикой full/incremental, совместимой с docker-compose.yml и docker compose ps.                                                          |
| 65 | Сборка минимального дашборда в Metabase                 | Уточнил, как построить минимум 3 визуализации по mart_yearly (линейный график, bar, таблица/area) и что показать на защите            | Получил пошаговый план: создать dashboard в Metabase, добавить line по gdp/year, bar по top‑5 и худшие 5 и table/area по gdp_growth_pct, и рекомендацию собрать 2–3 скриншота для docs/bi.                                         |
| 66 | Оформление docs/bi и подготовка к демонстрации BI‑части | Спросил, что именно показать на защите по docker-compose, volumes и дашборду, и как соотнести localhost и postgres внутри сети Docker | Получил структурированный чек‑лист: docker compose up/ps/volume ls, объяснение разницы down и down -v, Metabase → postgres vs localhost, а также как показать соответствие “CSV → Postgres → Metabase” и “контракт ↔ код” для mart |